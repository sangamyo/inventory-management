services:
  - type: web
    name: inventory-api
    runtime: docker
    plan: free
    rootDir: backend
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: inventory-db
          property: connectionString
      - key: CORS_ORIGINS
        sync: false

databases:
  - name: inventory-db
    plan: free
