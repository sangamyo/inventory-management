services:
  - type: web
    name: inventory-api
    runtime: docker
    plan: free
    rootDir: backend
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: inventory-db
          property: connectionString
      - key: CORS_ORIGINS
        value: https://frontend-ooedf0zg9-hari-om-kasaundhans-projects.vercel.app

databases:
  - name: inventory-db
    plan: free
